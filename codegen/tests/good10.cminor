/*
for statements
*/

main: function integer ( argc: integer, argv: array [] string ) =
{
        i: integer = 1;
        j: integer = 2;
        k: integer = 0;

        for (i = 0; i < 4; i++) {
                print i, '\n';
        }

        for (i = -4; i < j; i++)
                for (;j<5;j++)
                        for (;k <= 5;)
                        k++;
        print "iteratoin ", k, '\n';

        //for (m: integer = 0; m > -6; m--)
        for (i = 0; i > -6; i--)
                if (i < -2)
                        for (i = 0; i < 3; i++)
                                for (j = i; j< 3; j++)
                                        print j, '\n';


        return 0;
}